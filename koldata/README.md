# Koldata - Data gathering and storage core
These scripts are used to scrape twitter and the chain and gather a list of high value whales, influencers and 


## First time setup

From the repo home, create a venv `python3 -m venv .venv`, install requirements `pip install -r requirements.txt` and then run the agent with:

```
python scripts/ai_chain_analysis_agent.py
```

## .env file

```
OPENAI_API_KEY=key
APIFY_KEY=key
```